# Export security boundary

This repository is a source-only export of the ROTH DIGITAL web project.

It intentionally excludes environment files, API credentials, bot tokens, database credentials, payment wallet secrets, dependency directories, build output, local logs, Git metadata, and temporary artifacts. Configure deployment secrets only through the destination platform's protected secret manager.
