import StoreShell from "@/components/StoreShell";
import { MessageCircle, ShieldCheck, WalletCards } from "lucide-react";

const telegramSupportUrl = "https://t.me/the_stevenroths";

export default function SupportPage() {
  return <StoreShell><section className="support-page"><span className="catalog-kicker">SUPPORT / PRIVATE ACCESS DESK</span><h1>Need a clear<br /><em>next step?</em></h1><p>For order-specific help, keep your unique order ID available. Support can clarify a visible order state, product route, or payment handoff; it cannot approve a payment from the customer side.</p><div className="support-grid"><article><WalletCards size={22} /><h2>Order state</h2><p>Open your confirmation page and refresh the chain state before contacting support. Payment confirmation and manual review are separate stages.</p></article><article><ShieldCheck size={22} /><h2>Payment safety</h2><p>Never share a private key, recovery phrase, password, or exchange login. ROTH DIGITAL checkout only displays a public receiving address and an exact amount.</p></article><article><MessageCircle size={22} /><h2>Telegram support</h2><p>Message the verified ROTH DIGITAL support channel for order-specific help. Include your order ID and never send passwords, recovery phrases, or reusable payment credentials.</p><a className="button-primary" href={telegramSupportUrl} target="_blank" rel="noreferrer"><MessageCircle size={16} /> Message @the_stevenroths</a></article><article className="support-chat-placeholder"><MessageCircle size={22} /><h2>Live chat</h2><p>Live chat is intentionally unavailable until an approved provider and customer-service policy are configured. Use your order link or verified Telegram support channel instead.</p><span>Provider setup pending</span></article></div></section></StoreShell>;
}
