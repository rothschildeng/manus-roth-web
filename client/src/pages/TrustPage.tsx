import StoreShell from "@/components/StoreShell";
import { BadgeCheck, Eye, ShieldCheck, StarOff } from "lucide-react";
import { Link } from "wouter";

export default function TrustPage() {
  return <StoreShell><section className="trust-page"><span className="catalog-kicker">TRUST DESK / HOW ROTH WORKS</span><h1>Clear state.<br /><em>No invented signal.</em></h1><p className="trust-lede">ROTH DIGITAL shows a controlled access flow rather than making unverified speed, rating, or delivery claims. Payment records move through visible states from quote to blockchain confirmation to manual review.</p><div className="trust-grid"><article><ShieldCheck size={23} /><span>01 / PAYMENT QUOTE</span><h2>Exact amount.</h2><p>Crypto payment amounts are created server-side from the selected catalog item and network. The checkout screen shows the receiving address and time-limited amount before a customer sends funds.</p></article><article><Eye size={23} /><span>02 / CHAIN STATE</span><h2>Visible confirmation.</h2><p>Customers can refresh payment state as the selected network records confirmations. A confirmed payment does not silently become delivered.</p></article><article><BadgeCheck size={23} /><span>03 / REVIEW DESK</span><h2>Manual decision.</h2><p>Only confirmed orders reach the administrator’s protected review desk. Approval and final fulfillment remain deliberate actions.</p></article><article><StarOff size={23} /><span>REVIEWS / VERIFIED ONLY</span><h2>No fake ratings.</h2><p>Customer ratings and testimonials are not displayed until a real, verified source is connected. This storefront does not generate or seed customer feedback.</p></article></div><div className="trust-actions"><Link className="button-primary" href="/support">Contact support</Link><Link className="button-outline" href="/refunds">Read refund terms</Link></div></section></StoreShell>;
}
