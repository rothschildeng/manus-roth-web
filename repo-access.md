# Repository Access Note

On 14 August 2026, the provided URL `https://github.com/rothschildeng/roth-digital` returned GitHub's public `404 Page not found` screen in the active browser session. The page also displayed `Sign in`, so no authenticated repository contents were available. A non-interactive Git remote check likewise requested credentials. No pasted token was used or stored because credentials shared in chat must be treated as compromised.

Next safe step: the owner should revoke the exposed GitHub and bot tokens, then either authenticate the browser to the repository, provide a new public/correct repository URL, or upload an archive of the source files.
